{
  "name": "@capsulajs-web-components/request-form",
  "version": "0.0.1",
  "description": "CapsulaJS - Web components Request Form",
  "main": "lib/index.js",
  "unpkg": "dist/index.js",
  "module": "es/index.js",
  "types": "lib/index.d.ts",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rimraf node_modules && rimraf .cache && rimraf lib && rimraf es && rimraf dist && rimraf dist-min",
    "build": "rimraf .cache && npm-run-all build:cjs build:es build:umd build:umd-min",
    "build:cjs": "rimraf lib && tsc --module commonjs --target es2015 --outDir lib",
    "build:es": "rimraf es && tsc --module esnext --target es2015 --outDir es",
    "build:umd": "rimraf dist && parcel build src/index.ts --no-minify --experimental-scope-hoisting --out-dir dist --global logger",
    "build:umd-min": "rimraf dist-min && parcel build src/index.ts --experimental-scope-hoisting --out-dir dist-min --global logger",
    "cypress": "cypress run",
    "cypress:debug": "cypress open",
    "start": "rimraf dist && parcel start src/example/index.html",
    "lint": "tslint '{src,cypress}/**/*.{ts,tsx,js}' --fix",
    "prettier": "prettier --write '{src,cypress}/**/*.{ts,tsx,js}'",
    "test": "npm-run-all -p -r start cypress"
  },
  "author": "Markets Pulse",
  "dependencies": {
    "@capsulajs/capsulahub-ui": "0.5.6",
    "@capsulajs-web-components/utils": "0.0.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "rxjs": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^11.13.4",
    "@types/react": "^16.8.14",
    "@types/react-dom": "^16.8.4",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.3",
    "rimraf": "^2.6.3",
    "tslint": "^5.15.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-react": "^4.0.0",
    "typescript": "^3.4.3"
  }
}
