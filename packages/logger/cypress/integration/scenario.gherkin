Scenario: Events are logged (check the format)
  Given Logger web component
  When Logger is initialized
  Then at each polling interval of X seconds an event is logged
  And each log has the correct model
    | property    | type                 |
    | timestamp   | number               |
    | type        | 'request'/'response' |
    | serviceName | string               |
    | methodName  | string               |
    | data        | object               |
  Then the arrows icon have the correspondent color

Scenario: Check delete cross to remove all logs
  Given Logger web component
  And Logger is initialized
  When click on the delete cross
  Then all recorded logs are removed
  And logs continue to be recorded at each polling interval of X seconds

Scenario: Hovering on the dot near to log will change the color of all the dots of the logs with the same service/method name
  Given Logger web component
  And Logger is initialized
  And some logs are recorded
  When hover on the dot near to log
  Then the dot is changing its color
  And all the dots near to logs with the same service/method name are changing its color
