{
  "name": "@capsulajs/web-components-logger",
  "version": "0.0.1",
  "description": "CapsulaJS - Web components logger",
  "main": "./lib/index.js",
  "private": true,
  "capsula": {
    "template": "service"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf lib && cross-env BABEL_ENV=commonjs babel src/index.ts --out-dir lib --copy-files",
    "cypress": "cypress run",
    "start": "parcel start src/example/index.html",
    "lint": "tslint -p tsconfig.json --fix",
    "prettier": "prettier --write '{src,tests}/**/*.{ts,tsx}'",
    "test": "npm-run-all -p -r start cypress"
  },
  "author": "Markets Pulse",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "@types/node": "^11.13.4",
    "cross-env": "^5.2.0",
    "cypress": "^3.2.0",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.3",
    "rimraf": "^2.6.3",
    "tslint": "^5.15.0",
    "typescript": "^3.4.3"
  }
}
