sudo: true
language: node_js
node_js:
  - "10"

branches:
  only:
    - /.*/

install:
  - yarn install --production=false

before_script:
  - yarn bootstrap
  - yarn link

script:
  - yarn lint
  - yarn build
  - yarn test

notifications:
  email:
    on_failure: always
